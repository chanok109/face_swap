{
  "name": "face-swap-webapp",
  "version": "1.0.0",
  "description": "A simple face swap web app that proxies requests to the aifaceswap API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^17.4.2"
  }
}
